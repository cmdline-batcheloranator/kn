host local
   user apple
   hostname localhost
   identityFile ~/.ssh/local

host batchelorantor
   identityFile ~/.ssh/batchelorantor
	
host *
    user  git
    hostName  github.com
    preferredAuthentications publickey 

